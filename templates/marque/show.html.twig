{% extends 'base_admin_back.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ marque.marque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marque_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('app_marque_edit', {'id': marque.id}) }}" class="btn btn-warning">edit</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
